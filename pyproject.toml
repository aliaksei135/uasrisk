[build-system]
requires = [
    "setuptools",
    "wheel",
    "scikit-build>=0.13",
    "cmake",
    "ninja",
    "conan>=1.43.0",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]